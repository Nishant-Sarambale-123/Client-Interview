one java rest application. the job is containerized thw application. the url is also there where we can access the api. what components require. also write a docker file. (need to wirte docker file and all files require for deployment)

# Stage 1: Build the application
FROM maven:3.9-eclipse-temurin-17 AS builder
WORKDIR /build
COPY pom.xml .
COPY src ./src
RUN mvn clean package -DskipTests

# Stage 2: Run the application
FROM eclipse-temurin:17-jdk-alpine
WORKDIR /app
COPY --from=builder /build/target/*.jar app.jar

EXPOSE 8080

ENTRYPOINT ["java", "-jar", "/app/app.jar"]

Steps to Containerize & Run
1. Build Docker image
Shelldocker build -t myapp .Show more lines

2. Tag image (for pushing to Docker Hub)
Shelldocker tag myapp nishant123/myapp:v1Show more lines

3. Run container
Shelldocker run -d -p 8080:8080 nishant123/myapp:v1Show more lines

4. Access application
http://localhost:8080




